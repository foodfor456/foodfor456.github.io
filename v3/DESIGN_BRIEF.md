# 포트폴리오 v3 디자인 브리프 (2026-09-15, 디자이너 의견 반영)

> 현행 `/index.html`(문서형 v2, 9/13 배포)은 건드리지 않고 `/v3/index.html`에 새로 만든다. 디자이너 검토 후 교체.

## 디자이너 요구 (원문 요지)
1. 레퍼런스: 타이포그래피 중심, 구분감 있는 넓은 레이아웃(흑백 편집 디자인, 큰 한 단어 제목, [01] 번호, 가는 선, 검정 면 블록, 여백 넓게)
2. 흰 배경 + 어두운 글자. 폰트는 딱 2종.
3. GSAP 스크롤 애니메이션, Lenis 부드러운 스크롤.
4. 파비콘 직접 제작(클로드 로고로 보인 문제).
5. line-height 올려 가독성 확보.
6. 레이아웃 정돈, 인터랙션과 효과 추가.
7. 프로젝트 구간은 가독성과 구분 우선, 이미지 들어갈 자리를 잡아 둘 것(점선 플레이스홀더에 권장 크기 표기).
8. 점, 선, 면으로 된 디자인 요소를 더.

## 결정 (사용자 9/15 "디자이너 의견 그대로 진행")
- 한 페이지 스크롤(탭 폐지). 상단 고정 내비에 섹션 앵커.
- 콘솔은 검정 면 섹션 안에 작게 유지(타이핑 효과 그대로, 탭 대신 섹션 진입 시 재생).
- 큰 제목은 영문 한 단어(ABOUT, WORK, CASES, STACK, HISTORY, CONTACT), 한글 부제 병기.
- 이미지는 자리만 잡음(16:10, 점선 테두리, "이미지 1600x1000" 라벨). 공공 사이트 캡처는 나중에 사용자 확인 후 삽입.

## 스펙
- 컨테이너 1440px, 12칼럼 그리드, 좌우 여백 48px(모바일 20px).
- 색: 배경 #ffffff, 글자 #111111, 보조 #6b6b6b, 선 #e5e5e5, 면 #111111(검정 섹션에서는 글자 #ffffff). 포인트 컬러 없음(흑백).
- 폰트 2종: 본문과 한글 = Pretendard(로컬 woff2). 큰 제목과 숫자 = Archivo Black 또는 Anton(Google Fonts) 중 하나. 콘솔 모노스페이스는 시스템 폰트로 두되 "폰트 2종" 원칙상 UI 폰트로 세지 않음(디자이너 확인 필요, 문제 되면 콘솔도 Pretendard).
- 본문 17px, line-height 1.85. 표와 목록 1.7. 큰 제목 clamp(64px, 12vw, 200px), line-height 0.95.
- 섹션 구획: [01] 번호 + 소제목 + 가는 선(1px #e5e5e5). 섹션 사이 여백 160px.
- 점선면 요소: 점 격자(8px 간격 dot pattern, 배경 일부), 스크롤 진행에 따라 자라는 세로선, 검정 면 블록(콘솔과 신조 섹션), 마우스 따라오는 12px 점 커서(데스크톱만), 이미지 플레이스홀더 점선.
- 인터랙션: Lenis(smooth 1.1), GSAP ScrollTrigger로 제목 글자 단위 등장(SplitText 대신 span 분할), 섹션 페이드업, 프로젝트 행 hover 시 이미지 자리 밝아짐, 큰 제목 가로 이동(패럴랙스 약하게), 검정 면 섹션 핀. prefers-reduced-motion이면 전부 끔.
- 파비콘: "신" 한 글자 또는 "S" 검정 사각형에 흰 글자 SVG, favicon.svg + png 32.
- 크롤러 차단 메타와 robots.txt는 v2와 동일.

## 섹션 구성
1. HERO: 이름 "신의" 작게, 큰 타이포 "BACKEND" 두 줄(BACKEND / DEVELOPER), 아래 한 줄 소개(v2 헤드라인 "구축 15건, 동시 운영 11건. 같은 오류를 두 번 내지 않는 백엔드."), 오른쪽 하단 메일.
2. [01] ABOUT 일하는 방식: v2의 6개 문단을 2칼럼(왼쪽 소제목, 오른쪽 본문)으로. 6단계 원칙은 번호 목록.
3. [02] CASES 주요 사례 6건: 각 행 = 번호, 제목, 기간과 태그, 문제/접근/해결/성과, 오른쪽 이미지 자리.
4. [03] WORK 프로젝트: 구축 15건 목록(번호, 기간, 이름, 한 줄), 유지보수 11건, MES 7개사. 큰 제목 WORK.
5. 검정 면: 콘솔(projects.log tail) + 신조 세 줄.
6. [04] STACK 기술: 2칼럼 표를 목록형으로.
7. [05] HISTORY 이력: 2022 과정부터 2027.02 학사 예정까지 타임라인(세로선 + 점).
8. [06] CONTACT: 큰 메일 주소, 클릭 시 메일 앱과 복사.

## 콘텐츠 원칙 (v2와 동일)
- 사실만, v2 index.html의 문장을 그대로 옮긴다(숫자: 구축 15, 유지보수 11, MES 7, 경력 자동 계산).
- 가운뎃점과 대시 금지. 마더사와 파란샘 금지. 자축 문구 금지. AI 서술은 작업 2 + 검증 1 + PM 1까지.

## 작업 순서
1. `/v3/index.html` 골격과 CSS(그리드, 타이포, 색) → 로컬 서버로 확인.
2. 콘텐츠 이식(v2에서 복사) → 이미지 플레이스홀더.
3. GSAP + Lenis 인터랙션 → reduced-motion 처리.
4. 파비콘 → robots 메타 → 모바일 확인(iframe 400px).
5. push → 디자이너 검토(`https://foodfor456.github.io/v3/`) → 피드백 반영 → 확정 시 `/index.html` 교체.
